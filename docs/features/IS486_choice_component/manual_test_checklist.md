# IS486 — Ручной девайс-чек-лист (фазы 1–4)

Сценарии последовательные: данные **накапливаются** и переиспользуются, ничего не сбрасывать.
Прогонять сверху вниз. Отметка ✅ — пройдено.

---

## Подготовка A (базовая)

APK установлен. Если приложение стоит со старой базой (v11, тег 0.1.5) — НЕ переустанавливать
с очисткой: миграция 11→12 должна пройти на живых данных (M1). Если стоит свежая версия —
M1 пропустить, начать с M2.

---

## Блок 1 — миграция и builtin

- [ ] **M1. Миграция на живых данных.** Открыть приложение со старой базой.
  Ожидаемо: не падает; словари, слова, лексемы с переводами на месте; квиз-конфиги живы.
- ✅ **M2. Новый словарь.** Создать словарь `ES`.
  Ожидаемо: словарь создан, список слов пуст.
- ✅ **M3. Builtin в конструкторе.** Открыть конструктор (иконка-молоток в аппбаре словаря `ES`).
  Ожидаемо: одна лента, в ней «Перевод» (TEXT) и «Часть речи» (Выбор); у обоих есть свитч,
  НЕТ кнопок редактирования/удаления; кастомных пока нет.

## Блок 2 — карточка слова, базовый флоу

- ✅ **W1. Слово + перевод.** В `ES` создать слово `gato`, добавить лексему с переводом `кот`.
  Ожидаемо: лексема сохранена, Draft-бейдж исчез после первого значения.
- ✅ **W2. Часть речи через пикер.** В лексеме `gato` тапнуть чип «Часть речи».
  Ожидаемо: диалог с радио-вариантами (существительное, глагол, прилагательное, наречие,
  предлог, фраза); выбрать «существительное» — значение сохраняется сразу, без режима правки.
- ✅ **W3. Смена опции.** Удалить значение части речи крестиком, добавить заново с опцией
  «глагол» (решение 2026-07-21: тело чипа глухое, смена = удалить + добавить).
  Ожидаемо: значение сменилось на «глагол».

---

## Подготовка B (конструктор — компоненты для продвинутых сценариев)

В конструкторе `ES` создать, не удаляя ничего из блоков 1–2:

1. `Пример` — тип TEXT, «несколько значений» ВКЛ, зависимость: **Самостоятельный**, галка «Ядро» ВЫКЛ.
2. `Род` — тип Выбор, варианты: `мужской`, `женский`; зависимость: вложенная опция
   **существительное** (под «Часть речи»).
3. `Артикль` — тип TEXT, зависимость: компонент **Род**.

- ✅ **P1.** Все три создались; у `Род` в диалоге создания при выборе «Выбор»
  чекбокс «несколько» пропадает; попытка создать Выбор без вариантов — ошибка
  «Добавьте хотя бы один вариант», диалог не закрывается.

## Блок 3 — иерархия в карточке (закон доступности)

- ✅ **H1. Зависимый закрыт без цели.** В лексеме `gato` (часть речи = «глагол» после W3):
  чип `Род` НЕ предлагается, `Артикль` НЕ предлагается, `Пример` предлагается.
- ✅ **H2. Опция открывает зависимого.** Сменить часть речи на «существительное».
  Ожидаемо: появился чип `Род`; `Артикль` всё ещё нет.
- ✅ **H3. Цепочка.** Выбрать `Род` = «мужской».
  Ожидаемо: появился чип `Артикль`; добавить значение `el`.
- ✅ **H4. Каскад по смене опции.** Сменить часть речи на «фраза».
  Ожидаемо: значения `Род` и `Артикль` исчезли (сброшены каскадом); перевод и часть речи живы.
- ✅ **H5. Мульти-цель.** Вернуть «существительное», снова `Род` = «мужской», `Артикль` = `el`.
  Добавить два значения `Пример` (`el gato negro`, `gato callejero`). Удалить одно.
  Ожидаемо: второе живо, ничего лишнего не каскадировало.
- ✅ **H6. Деградация в черновик.** Создать слово `sol`, лексему с одним переводом.
  Удалить значение перевода.
  Ожидаемо: лексема НЕ удалилась — карточка осталась с Draft-бейджем и чипами; undo-снека нет.
  Примечание (решение 2026-07-21): черновик живёт ТОЛЬКО пока карточка открыта —
  при выходе/повторном входе пустая сохранённая лексема удаляется тихо.

## Блок 4 — рубильник enabled

- ✅ **E1. Выключение не-ядра.** В конструкторе выключить свитч `Часть речи`.
  Ожидаемо: ряд приглушён, чип «Выключен»; в карточке `gato` существующее значение
  части речи ВИДНО и живо, `Род`/`Артикль` работают; в новой лексеме чип «Часть речи»
  НЕ предлагается.
- ✅ **E2. Последнее ядро.** Попытаться выключить `Перевод` (единственное включённое ядро —
  кастомных ядер в `ES` нет).
  Ожидаемо: отказ со снеком про последнее ядро; свитч остался включён.
- ✅ **E3. Обратно.** Включить `Часть речи` назад — чип снова предлагается в карточке.

## Блок 5 — редактирование и опции

- ✅ **O1. Rename варианта.** Edit `Род` → переименовать `мужской` → `муж.` → Сохранить.
  Ожидаемо: в карточке `gato` значение рода показывает `муж.`.
- ✅ **O2. Добавить вариант.** Edit `Род` → «Добавить вариант» → `средний` → Сохранить.
  Ожидаемо: в пикере рода три варианта.
- ✅ **O3. Удаление варианта с impact.** Edit `Род` → крестик у `муж.`.
  Ожидаемо: конфирм с числом скрываемых значений (выбор рода + зависимый `Артикль`);
  подтвердить → опция исчезла из списка, в карточке `gato` значения `Род` и `Артикль`
  скрыты каскадом. «Не работает» НЕ появляется — `Артикль` зависит от компонента
  `Род`, а не от удалённой опции (деградируют только зависимые от самой опции).
- ✅ **O4. Лечение degraded перепривязкой.** Edit `Артикль` → зависимость: компонент `Род` →
  (уже так) — тогда перепривязать на **Самостоятельный** → Сохранить.
  Ожидаемо: чип «Не работает» исчез.
- ✅ **O5. Builtin нередактируем.** У «Часть речи» и «Перевод» нет кнопки редактирования —
  варианты части речи изменить нельзя.

## Блок 6 — перепривязка и циклы

- ✅ **C1. Умный сброс при перепривязке (решение 2026-07-21).** У лексемы `gato`
  есть значение `Пример`. Edit `Пример` → зависимость: компонент `Перевод` → Сохранить.
  Ожидаемо: конфирм «Безопасно: существующие значения не пострадают» → подтвердить →
  значения `Примера` ЖИВЫ (перевод есть в лексеме — условие выполнено).
- ✅ **C1b. Небезопасная перепривязка.** Edit `Пример` → зависимость: вложенная опция
  `жен.` (под `Род`) → Сохранить.
  Ожидаемо: конфирм «Будет скрыто значений: N» (род у `gato` не «жен.») → подтвердить →
  значения `Примера` скрыты; вернуть `Пример` на «Самостоятельный» (конфирм покажет
  «безопасно»).
- ✅ **C2. Цикл — дизейбл в пикере (решение 2026-07-21).** Edit `Артикль` →
  зависимость: компонент `Род` → Сохранить (конфирм). Затем Edit `Род` → пикер цели.
  Ожидаемо: ряд «Артикль» приглушён и НЕ тапается, под ним красная подпись
  «Нельзя выбрать — возникнет цикл зависимостей».
- ✅ **C3. Удаление с поддеревом.** Удалить компонент `Род` (кнопка-корзина).
  Ожидаемо: конфирм с impact; после — `Артикль` живой, но с чипом «Не работает»;
  значения поддерева скрыты из карточек.

## Блок 7 — квизы и навигация

- ✅ **Q1. Квиз живёт.** Прогнать квиз по `ES` (перевод).
  Ожидаемо: работает как раньше; часть речи (Выбор) в пикере компонентов квиза НЕ предлагается.
- ✅ **Q2. Manager недостижим.** Настройки: пункта «конструктор компонентов» НЕТ;
  вход в компоненты — только молоток в аппбаре словаря.
- ✅ **Q3. Второй словарь изолирован.** Создать словарь `DE`.
  Ожидаемо: у него свои builtin (свитчи независимы от `ES` — выключить «Часть речи» в `DE`,
  в `ES` она остаётся включённой); кастомные компоненты `ES` в конструкторе `DE` не видны.

---

## Итог

- ✅ Все блоки пройдены, приложение не падало.
- Замечания:
