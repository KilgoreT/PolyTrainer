# Findings: повторное ревью плана (после 27 первых правок)

Два ревью: Полнота, Консистентность. Findings отфильтрованы.

---

## КРИТИЧНЫЕ

### 1. MateTypedEffectHandler отсутствует в Этапе 1
**Статус:** ✅ обработано — в Этап 1 добавлен пункт 1 (создать `MateTypedEffectHandler.kt`).

### 2. Порядок шагов в Этапе 2 нелогичен
**Статус:** ✅ обработано — порядок изменён: build.gradle → handler → удалить @Binds → добавить getter → ViewModel → удалить ручное создание. Каждый промежуточный коммит собирается.

### 3. Splash в таблице порядка без ExitApp
**Статус:** ✅ обработано — в таблицу добавлен ExitApp в зависимости Splash.

### 4. Cleanup не упоминает обновление guides/effect-handlers.md
**Статус:** ✅ обработано — в Этап 4 добавлен пункт 7 (обновить пример NavigationEffectHandler в гайде).

### 5. Удаление NavigationEffect.ExitApp без инвентаризации usages
**Статус:** ✅ обработано — в Этап 1 пункт 4 добавлена инвентаризация через grep + замена на per-screen sealed.

---

## ВАЖНЫЕ

### 6. Cross-graph секция показывает только VocabularyNavigator
**Статус:** ✅ обработано — добавлены контракты Navigator для QuizTab, StatisticTab, SettingsTab.

### 7. Особые случаи Этапа 2 не отражены в таблице
**Статус:** ✅ обработано — в таблицу добавлена колонка "Особые работы" с пометками для DictionaryForm (FilterFlags), Chat (crash fix), QuizTab/StatisticTab (удалить пустой handler), WordCard (двойная правка).

### 8. Пример "ДО" в "Идея" не отражает реальный код
**Статус:** ✅ обработано — добавлен `onExit` в пример конструктора и в создание ListNavigationEffectHandler.

### 9. Unchecked cast `effect as E` без объяснения safety
**Статус:** ✅ обработано — добавлен комментарий в коде про safe semantics + сценарий ClassCastException.

### 10. `onScreenEffect` vs `onEffect` — переименование не объяснено
**Статус:** ✅ обработано — добавлен комментарий в код базового класса с объяснением final + новый абстрактный метод без consumer.

---

## ЗАМЕЧАНИЯ

### 11. Заголовок "Commands интерфейс" — остаток от старой концепции
**Статус:** ✅ обработано — заменён на "Navigator интерфейс".

### 12. Дубль в "Что в итоге"
**Статус:** ✅ обработано — секция переписана, дубли объединены, добавлены пропущенные пункты (conditional навигация, launchSingleTop, appBarNavigator).

### 13. Boilerplate per-screen — 6 vs 7 точек
**Статус:** ✅ обработано — исправлено на 7.

### 14. VocabularyTabScreen с двумя navigator-параметрами не описан в Этап 3
**Статус:** ✅ обработано — в Этап 3 пункт 5 добавлено правило про `appBarNavigator` для табов с shared widget.

### 15. consumer параметр — расхождение с гайдом
**Статус:** ❌ отклонено — в гайде эффект-хендлеров уже есть исключение для navigation handler'а (consumer не вызывается). Навязчивое уточнение.

### 16. BottomBar не упомянут в shared widgets
**Статус:** ✅ обработано — добавлен пункт: BottomBar Navigator не нужен, использует tabsNavController напрямую.

### 17. Дубль "Что отвергнуто" / "Что в итоге"
**Статус:** ❌ отклонено — разные роли секций (отклонённые варианты vs финальная картина). Не дублируют.

---

## Сводная таблица

| # | Проблема | Уровень | Статус |
|---|----------|---------|--------|
| 1 | MateTypedEffectHandler отсутствует в Этапе 1 | КРИТ | ✅ |
| 2 | Порядок шагов в Этапе 2 нелогичен | КРИТ | ✅ |
| 3 | Splash в таблице без ExitApp | КРИТ | ✅ |
| 4 | Cleanup не упоминает effect-handlers.md | КРИТ | ✅ |
| 5 | Удаление ExitApp без инвентаризации usages | КРИТ | ✅ |
| 6 | Cross-graph только VocabularyNavigator | ВАЖН | ✅ |
| 7 | Особые случаи Этапа 2 не в таблице | ВАЖН | ✅ |
| 8 | Пример "ДО" без onExit | ВАЖН | ✅ |
| 9 | Unchecked cast без объяснения | ВАЖН | ✅ |
| 10 | onScreenEffect vs onEffect переименование не объяснено | ВАЖН | ✅ |
| 11 | "Commands интерфейс" — остаток | замечание | ✅ |
| 12 | Дубль в "Что в итоге" | замечание | ✅ |
| 13 | Boilerplate 6 vs 7 точек | замечание | ✅ |
| 14 | VocabularyTabScreen с appBarNavigator не описан | замечание | ✅ |
| 15 | consumer — расхождение с гайдом | замечание | ❌ отклонено |
| 16 | BottomBar не упомянут | замечание | ✅ |
| 17 | "Что отвергнуто" / "Что в итоге" дубль | замечание | ❌ отклонено |
